# Changes

Create one folder per proposed behavior change.

Each change should include a `proposal.md` with:

- problem;
- proposed behavior;
- affected apps/packages;
- scenarios;
- verification commands.

Archive or delete stale proposals once the accepted spec under `openspec/specs/` has been updated.
